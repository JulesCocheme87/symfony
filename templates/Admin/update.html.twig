{% extends 'base.html.twig' %}
{% block title %}Modifier le produit{% endblock %}

{% block body %}
    <h1>Modifier : {{ produit.nom }}</h1>
    {{ form_start(my_form) }}
        {{ form_widget(my_form) }}
    {{ form_end(my_form) }}
{% endblock %}
